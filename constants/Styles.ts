import { StyleSheet } from "react-native";

export const Styles = StyleSheet.create({
  backgroundImage: {
    position: "absolute",
    bottom: 0,
    right: 0,
    width: "100%",
    height: "100%",
    opacity: 0.5,
    zIndex: -1,
  },
});
